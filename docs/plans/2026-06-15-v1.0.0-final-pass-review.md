# v1.0.0 Final-Pass Review — Solutions PR #347

**Date:** 2026-06-15 · **Branch:** `solutions/connection-references` (tip after fixes: see commit list) · **Reviewer:** Claude (Opus 4.8), swarm + independent verification.
**Scope reviewed:** `v0.9.1..HEAD` — 652 commits / 96 PRs / ~103K LOC / 870 files. Solutions + adjacent (external-user model, v2 app model, sandbox hardening, CI/release-channel changes).

## VERDICT: GO-WITH-FIXES → now GO (all gating fixes applied on-branch)

Every gating issue found was **test/mirror/infra-only, zero product-behavior risk**, and all are fixed on the branch. The Solutions feature itself is ship-ready: org-scoping contract holds, the always-on read-only `before_flush` guard is installed in prod and deploy writes use Core statements, the migration chain is single-headed and round-trips on a populated DB, and the consequential prior fixes (git-sync data-loss H1, connection-schema threading, local-dev solution_id) are genuinely fixed (not false-green).

## Gating issues found AND FIXED on-branch

| ID | Issue | Found by | Fix (commit) |
|----|-------|----------|--------------|
| B1 | Stale `.codex/skills/bifrost-release` mirror trips the new CI lint Codex-mirror gate → blocks merge AND tag build. Root cause: the cadence-policy section I added to the canonical skill wasn't mirrored. | swarm (CI lane), reproduced live | `scripts/sync-codex-skills.sh` + commit `4afb0237`. Mirror now clean. |
| B2 | `test_platform_api_docs.py` pointed at old `platform-api.md`; the build-skill rework moved it to `references/platform-api.md`. Runs in the unit gate → CI red. | my `./test.sh all` run | path fix, commit `ca9dabbf`. Verified pass. |
| B3 | `test_cli_org_flags::test_forms_create_org_forms` missing now-required `--form-schema` flag (DTO-required-flags change). Unit gate → CI red. | my `./test.sh all` run | added `--form-schema '{"fields": []}'`, commit `ca9dabbf`. Verified pass. |

## Retracted / non-issues (verified, do NOT act on)

- **V-1 (SafeHTMLRenderer 6 vitest failures): RETRACTED.** Not a regression and not happy-dom. Test + component + vitest env are byte-identical to main; failures are a **local Node 24.13.0 vs CI Node 20** difference in happy-dom's `insertBefore`/`WHOLE_DOCUMENT` behavior. **Main CI is green** on Client Unit Tests (Node 20). No fix needed.
- **SDK-build e2e failures** (`test_sdk_package`, `test_solution_app_build`, PermissionError on `sdk_src/`): **local stale-test-image artifact.** In CI the test-runner image bakes `sdk_src/index.ts` (anonymous volume preserves it) so `_ensure_sdk_src()` short-circuits; locally the host `sdk_src/` has only `.gitkeep` so the helper tries to write and the hardened container refuses. `./test.sh stack down && up` rebuilds and clears it. Not a branch defect; these are `@pytest.mark.e2e` and CI's unit gate excludes e2e.
- **Different-each-run unit failures** during manual `docker compose run`: dirty test DB (unique-violation on `uq_workflows_path_function_repo`) because the manual workaround skips `./test.sh`'s auto state-reset. Gone after `./test.sh stack reset` + a proper `./test.sh unit` run (4672 passed, only the 2 e2e-image artifacts above).

## Quality gates (final, clean runs)

- `ruff check` (api) — PASS · `pyright` (api) — PASS · client `tsc` — PASS · client `lint` — PASS (1 pre-existing FormRenderer warning)
- `./test.sh unit` (auto-reset) — **4672 passed**, 2 failed = the local SDK-image artifact only (green in CI)
- `./test.sh all` — green apart from the same local artifacts
- client vitest — green in CI (Node 20); 6 local failures are the Node-24 artifact above
- docs site (`bifrost-integrations-docs`) — manifest lint + astro build PASS

## Non-blocking follow-ups (file as issues; do NOT gate v1.0.0)

1. **Slow-marked unit files run in zero CI** — `test_import_hygiene.py` (the `src.*`-in-CLI guard), `execution/test_template_process.py`, `test_github_sync.py::TestMemoryUsageDuringFileScan`. `cmd_unit` adds `-m "not slow"` and the claimed nightly workflow doesn't exist. Add a scheduled `./test.sh all`, or un-slow the import-hygiene guard. (medium)
2. **Cross-org existence/managed oracle** — `assert_entity_id_not_solution_managed` runs an org-unscoped lookup before the org-scoped repo authorizes on `applications.py` mutation routes; a cross-org PATCH gets 409 (discloses existence) vs 404. Resolve+authorize first. (low, info-disclosure)
3. **`before_flush` backstop blind to junction/collection writes** — mitigated at every audited router by explicit guards; only reachable by a future unguarded surface. Soften the docstring or extend coverage. (low)
4. **Dead module `ref_rewriter.py`** (`WorkflowRefRewriter` et al.) — zero prod callers (test-only); the migrate CLI emits TODO markers instead. Delete or wire it in (no-dead-code rule). (low)
5. **build skill two unsymlinked copies** — byte-identical now, will drift. Collapse via the `skills/build` symlink. (low)
6. **CLAUDE.md skill-truth CI claim** is inaccurate (enforced by a unit test, not a workflow step). (info)
7. **Plugin manifest** at `0.9.2-dev.587` — must bump to `1.0.0` before tagging (tag-time guard); this is the release-skill step, not a merge gate.
8. **Screenshot capture** for the new Solutions docs is deferred (placeholders left) — needs a port-mode debug stack and post-merge UI.

## Coverage caveats to state in release notes (Solutions V1)

Knowledge/RAG corpora, form/table file uploads, and private-repo install are **not** bundled/supported in v1.0.0. Agents reference knowledge by namespace name and the namespace must pre-exist in the target environment.

## Merge decision (for Jack)

The branch is mergeable: GO-WITH-FIXES, and all three gating fixes are committed. **Per your instruction I have NOT merged** — this is the stop-and-report point. Recommended sequence: (1) you review/approve these 3 fix commits; (2) bump plugin manifest to 1.0.0; (3) merge #347 to main; (4) cut `v1.0.0`. The release notes are drafted separately.
